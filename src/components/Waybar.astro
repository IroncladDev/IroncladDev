---
import { ScreenName } from '@/types/state'
---

<row id="waybar" gap-="1">
    <div id="icon"></div>
    <row id="workspaces">
        <a class="workspace" href={ScreenName.About}>
            <span is-="badge">1</span>
            <span is-="badge" variant-="background2" cap-="square round"
                >Info</span
            >
        </a>
        <a class="workspace" href={ScreenName.Projects}>
            <span is-="badge">2</span>
            <span is-="badge" variant-="background2" cap-="square round"
                >Projects</span
            >
        </a>
        <a class="workspace" href={ScreenName.Contact}>
            <span is-="badge">3</span>
            <span is-="badge" variant-="background2" cap-="square round"
                >Links</span
            >
        </a>
        <a class="workspace" href={ScreenName.Blog}>
            <span is-="badge">4</span>
            <span is-="badge" variant-="background2" cap-="square round"
                >Opinions</span
            >
        </a>
    </row>
</row>

<style>
    #icon {
        height: 1lh;
        width: 2ch;
        background-image: url('/img/brand/svg/icon.svg');
        background-size: contain;
        background-repeat: no-repeat;
        background-position: center;
    }

    #waybar {
        background-color: var(--surface1);
        padding: 0.5lh 2ch;
        #workspaces {
            flex-wrap: wrap;
            gap: 0 1ch;
        }
    }

    .workspace {
        text-decoration: none;
        display: inline-flex;

        & > span:first-child {
            --badge-color: var(--background3);
            color: var(--foreground0);
        }

        html:has(app-state[screen-='#about']) &[href='#about'],
        html:has(app-state[screen-='#projects']) &[href='#projects'],
        html:has(app-state[screen-='#contact']) &[href='#contact'],
        html:has(app-state[screen-='#blog']) &[href='#blog'] {
            & > span:first-child {
                --badge-color: var(--green-fg);
                color: var(--root);
            }
        }
    }
</style>

<script>
    import { ScreenName } from '@/types/state'
    import { openScreen } from '@/utils/navigation'

    const onHashChange = () => {
        const derivedScreen =
            Object.values(ScreenName).find(
                (name) => name === window.location.hash,
            ) ?? ScreenName.About
        document
            .querySelector('app-state')
            ?.setAttribute('screen-', derivedScreen)
        openScreen(derivedScreen)
    }

    window.addEventListener('hashchange', onHashChange)
    window.addEventListener('keydown', (e) => {
        if (e.key === '1') window.location.hash = ScreenName.About
        else if (e.key === '2') window.location.hash = ScreenName.Projects
        else if (e.key === '3') window.location.hash = ScreenName.Contact
        else if (e.key === '4') window.location.hash = ScreenName.Blog
    })
</script>
